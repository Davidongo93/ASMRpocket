import { StyleSheet, Text, View } from 'react-native';

const LikedSongsScreen = () => {
  return (
    <View>
      <Text>Liked Songs Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default LikedSongsScreen;
